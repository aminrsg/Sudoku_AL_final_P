package solver;

public class CSPSolver extends SudokuSolverBase {

    public CSPSolver(int[][] board) {
        super(board);
    }

    @Override
    public int[][] solve() {
        if (solveSudoku()) {
            return board;
        }
        return null;
    }
}
